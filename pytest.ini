[pytest]
markers =
    slow: marks tests that take >5s (deselect with -m "not slow")
    integration: integration tests requiring real model weights or external data
testpaths =
    whales_be_service/tests
    whales_identify/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

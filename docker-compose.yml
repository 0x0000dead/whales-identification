services:
  backend:
    image: ecomarine-backend:latest
    build:
      context: ./whales_be_service
      dockerfile: Dockerfile
      args:
        MODEL_DOWNLOAD_URL: "${MODEL_DOWNLOAD_URL:-}"
    container_name: ecomarine-be
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      # "*" — dev default: the UI must work when opened from any machine on
      # the network, not just localhost. Narrow this to an explicit origin
      # list for production deployments.
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-*}
    # Named volumes keep model files between container restarts.
    # First boot downloads ~2 GB from HuggingFace; subsequent starts are instant.
    volumes:
      - ecomarine_models:/app/src/whales_be_service/models
      - ecomarine_resources:/app/src/whales_be_service/resources
      - ecomarine_configs:/app/src/whales_be_service/configs
      - ecomarine_reports:/app/reports
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 300s  # allow up to 5 min for first-boot model download
    networks:
      - whale-net

  frontend:
    image: ecomarine-frontend:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Empty by default: the frontend then resolves the backend at runtime
        # as http://<host the UI is opened from>:8000, so the stack works from
        # any device on the network. Set VITE_BACKEND only for reverse-proxy
        # setups or a non-standard backend port.
        VITE_BACKEND: "${VITE_BACKEND:-}"
        NPM_REGISTRY: "${NPM_REGISTRY:-https://registry.npmjs.org}"
    container_name: ecomarine-fe
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - whale-net

networks:
  whale-net:
    driver: bridge

# Persistent volumes — survive `docker compose down`, removed only by `docker compose down -v`
volumes:
  ecomarine_models:
  ecomarine_resources:
  ecomarine_configs:
  ecomarine_reports:
